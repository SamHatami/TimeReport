<UserControl
    x:Class="Zurvan.ClientApp.Views.UserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro.Platform"
    xmlns:converters="clr-namespace:Zurvan.ClientApp.Views.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:Zurvan.ClientApp.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:UserViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="{StaticResource Main.Background}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:ProjectViewConverter x:Key="ProjectViewConverter" />
        <converters:UserTimeConverter x:Key="TImeConverter" />
    </UserControl.Resources>
    <Grid Margin="5" Background="{StaticResource Main.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.15*" />
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <DatePicker
            Margin="25,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center" />

        <TextBox
            Grid.Row="0"
            Margin="160,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Text="{Binding User.FirstName, Mode=TwoWay}"
            TextWrapping="Wrap" />

        <!--<ListView
            x:Name="TimeReport"
            Grid.Row="1"
            ItemsSource="{Binding ProjectViewModels, Mode=TwoWay}"
            SelectionMode="Extended">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Margin" Value="5" />
                    <Setter Property="Padding" Value="5" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ContentControl cal:View.Model="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />

                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>-->

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label Width="200" Content="Project" />
            <Label Content="Monday" />
            <Label Content="Tuesday" />
            <Label Content="Wednesday" />
            <Label Content="Thursday" />
            <Label Content="Friday" />

        </StackPanel>

        <ListView
            x:Name="Test"
            Grid.Row="2"
            ItemsSource="{Binding Projects}"
            SelectionMode="Extended">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Margin" Value="5" />
                    <Setter Property="Padding" Value="5" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <StackPanel.Resources>
                            <Style TargetType="{x:Type TextBox}">
                                <Setter Property="Margin" Value="0,0,10,0" />
                            </Style>
                        </StackPanel.Resources>
                        <Label Width="200" Content="{Binding Path=Name}" />
                        <TextBox Width="40" Text="{Binding UserDateTimeReported, Converter={StaticResource TImeConverter}, ConverterParameter='Monday'}" />
                        <TextBox Width="40" Text="{Binding UserDateTimeReported, Converter={StaticResource TImeConverter}, ConverterParameter='Tuesday'}" />
                        <TextBox Width="40" Text="{Binding UserDateTimeReported, Converter={StaticResource TImeConverter}, ConverterParameter='Wednesday'}" />
                        <TextBox Width="40" Text="{Binding UserDateTimeReported, Converter={StaticResource TImeConverter}, ConverterParameter='Thursday'}" />
                        <TextBox Width="40" Text="{Binding UserDateTimeReported, Converter={StaticResource TImeConverter}, ConverterParameter='Friday'}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>


        <!--<StackPanel x:Name="DataPanel" Grid.Row="1">
            <StackPanel x:Name="HeaderPanel" Orientation="Horizontal">
                <Label Content="Projects" />
            </StackPanel>
        </StackPanel>-->
    </Grid>
</UserControl>

<!--
    cal:Action.TargetWithoutContext="{Binding ElementName=TimeReport, Path=DataContext}"
    AutoGenerateColumns="False"
    CanUserAddRows="False"
    CanUserDeleteRows="False"
    CanUserReorderColumns="False"
    CanUserResizeColumns="False"
    CanUserResizeRows="False"
    CanUserSortColumns="False"
    SelectionMode="Single"
    Style="{StaticResource DataGridStyle}"
    SelectedItem="key"
-->